;
; .emacs file. the work is done in my-init.el and other personalization files.
;
; David Kotz (Kotz@Dartmouth.edu)
;

(defvar user-home-directory)
(setq user-home-directory (getenv "HOME"))
(add-to-list 'load-path (concat user-home-directory "/lib/emacs"))

(load "my-init")
